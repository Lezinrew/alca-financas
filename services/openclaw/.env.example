# OpenClaw - variáveis de ambiente (cópia para .env fora do repositório)
# Copiar: cp .env.example .env e preencher valores.
# Nunca commitar .env.

# Token do gateway (obrigatório). Gerar: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=

# Diretórios no host (usados apenas se rodar compose standalone em services/openclaw)
# OPENCLAW_CONFIG_DIR=./data/openclaw/config
# OPENCLAW_WORKSPACE_DIR=./data/openclaw/workspace

# Opcional: expor UI apenas em localhost (descomente ports em docker-compose)
# OPENCLAW_GATEWAY_PORT=18789

# Provedores LLM (opcional; configurável também via CLI/onboarding)
# CLAUDE_AI_SESSION_KEY=
# CLAUDE_WEB_SESSION_KEY=
# CLAUDE_WEB_COOKIE=
