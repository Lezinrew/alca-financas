You are generating a **single Git commit message** for the Alça Finanças repository.

Context you may read:
- `skills/README.md`
- `skills/SKILLS_REGISTRY.md`
- The staged diff only (for example: `git diff --cached`).

Do **NOT** scan the entire repository or unrelated files.

Constraints:
- Use a short Conventional Commit style:
  - `feat: ...`, `fix: ...`, `docs: ...`, `chore: ...`, `refactor: ...`, `test: ...`
- Focus on **why** the change was made, not every tiny detail of what changed.
- Maximum 1 short subject line and, optionally, a brief body (wrapped at ~72 chars).

Output format:
- First line: `<type>: <short summary>`
- Optional body: one or two short paragraphs, separated by a blank line.

